import { Container } from "@/src/components/layout/Container";
import { Accordion } from "@/src/components/ui/Accordion";
import { SectionHeading } from "@/src/components/ui/SectionHeading";

const items = [{ question: "Does JourneyCard apply for the card for me?", answer: "No. We help you compare and understand options; any application happens directly with the issuer." }, { question: "Do I need a credit check to use the matcher?", answer: "No. The matcher is free and does not run a credit check." }, { question: "How do you handle changing card benefits?", answer: "We show confidence levels and source links, and clearly flag details that need confirmation with the issuer." }, { question: "What does lifetime free mean?", answer: "It means the card currently has no joining or annual fee in the data we have verified. Issuer eligibility still applies." }, { question: "Can I compare more than one card?", answer: "Yes. Add cards from Top Picks and use the comparison section to review them side by side." }, { question: "Is JourneyCard financial advice?", answer: "No. It is an educational comparison tool. Always read the issuer's current terms before applying." }];
export function Faq() { const jsonLd = { "@context": "https://schema.org", "@type": "FAQPage", mainEntity: items.map((item) => ({ "@type": "Question", name: item.question, acceptedAnswer: { "@type": "Answer", text: item.answer } })) }; return <section id="faq" className="py-20 md:py-28"><Container className="max-w-3xl"><SectionHeading eyebrow="Questions, answered" title="Before you choose." description="The useful details, without the fine-print theatre." align="center" /><div className="mt-10"><Accordion items={items} /></div><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }} /></Container></section>; }
