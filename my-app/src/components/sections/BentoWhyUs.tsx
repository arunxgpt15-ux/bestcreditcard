import { GitCompareArrows, Gem, Sparkles } from "lucide-react";
import { Container } from "@/src/components/layout/Container";
import { SectionHeading } from "@/src/components/ui/SectionHeading";
import { CountUp } from "@/src/components/ui/CountUp";

const items = [{ title: "Smart matching", text: "Your priorities shape the shortlist, not a generic popularity list.", icon: Sparkles }, { title: "Hidden perks", text: "We surface lounge access, milestones, and watch-outs in plain language.", icon: Gem }, { title: "Instant comparison", text: "Put fees, forex, and travel value side by side before you apply.", icon: GitCompareArrows }];
export function BentoWhyUs() { return <section id="why-us" className="py-20 md:py-28"><Container><SectionHeading eyebrow="A smarter way to choose" title="Less tab-hopping. More confidence." description="JourneyCard makes the trade-offs visible, so the right card feels obvious." align="center" /><div className="mt-12 grid gap-4 md:grid-cols-2 lg:grid-cols-4"><div className="glass-card relative overflow-hidden p-7 md:col-span-2 lg:row-span-2"><p className="eyebrow">Value discovered</p><p className="display-heading mt-14 text-6xl text-ink"><CountUp value={42000} prefix="₹" /></p><p className="mt-5 max-w-sm text-sm leading-7 text-muted">Average yearly value surfaced across our premium travel matches, before taxes and redemption conditions.</p><div className="absolute bottom-0 right-0 h-40 w-40 rounded-full bg-teal-400/10 blur-3xl" /></div>{items.map(({ title, text, icon: Icon }) => <article key={title} className="glass-card p-6 transition hover:border-teal-400/50"><span className="inline-flex h-11 w-11 items-center justify-center rounded-xl bg-teal-400/10 text-teal-400"><Icon aria-hidden="true" className="h-5 w-5" /></span><h3 className="mt-7 font-bold text-ink">{title}</h3><p className="mt-2 text-sm leading-6 text-muted">{text}</p></article>)}</div></Container></section>; }
